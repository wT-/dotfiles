# TODO:

# $HOME = Drive:\Users\[USER]
# $HOME\[My ]Documents = [Environment]::GetFolderPath("MyDocuments")
# PowerShell:
# Symlink all files from "powershell" to "$Documents/PowerShell" and "$Documents/WindowsPowerShell" for older PowerShell versions
# Symlink contents of "powershell/modules"
# Symlink ".config/pshazz" to "$HOME/.config/pshazz"

# Starship:
# Symlink ".config/starship.toml" to $HOME/.config/starship.toml

# Sublime Text 3:
# Copy the "User" dir to "$HOME/AppData/Roaming/Sublime Text 3/Packages/User"
# TODO: Should it be a symlink instead? I remember having problems with symlinks but
# I can't remember what...
